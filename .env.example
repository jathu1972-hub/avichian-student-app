# Local development: leave empty — Vite proxies /api and /socket.io → http://127.0.0.1:4000
VITE_API_URL=

# Production (Netlify build): set in Netlify UI (not only this file) and Redeploy.
# Origin only — code appends /api automatically.
# VITE_API_URL=https://api.avichian.in
